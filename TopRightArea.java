import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class TopRightArea here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class TopRightArea extends FireArea
{

    /**
     * Constructor for objects of class TopRightArea.
     * 
     */
    public TopRightArea()
    {    
      addObject(new FireOrb(), 250, 250);
    }
}
