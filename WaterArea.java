import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class WaterArea here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WaterArea extends Overworld
{

    /**
     * Constructor for objects of class WaterArea.
     * 
     */
    public WaterArea()
    {
      addObject(new WaterSlime(), 165, 155);
    }
}
